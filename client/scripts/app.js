var app = angular.module("demoApp", []);
app.config(function($routeProvider) {
  // $routeProvider
  // .when("/login", {
  //   templateUrl : "views/login.html",
  // })
  // .when("/red", {
  //   template : "<h1>Test</h1>"
  // })
  // otherwise('/login');

  
});